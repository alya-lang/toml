# Types and data structures for toml
# Part of the Alya Language package ecosystem (https://github.com/alya-lang)

struct TomlConfig
    name
    prefix
    count
end

function make_config(name = "World", count = 1)
    return TomlConfig {
        name: name,
        prefix: "Hello",
        count: count
    }
end